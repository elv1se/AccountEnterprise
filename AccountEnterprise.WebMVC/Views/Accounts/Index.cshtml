@model IEnumerable<AccountEnterprise.Application.Dtos.AccountDto>
@{
    Layout = "_Layout";
}

<h2>Счета</h2>

<a asp-controller="Accounts" asp-action="Create">Создать новый счет</a>
<table class="table">
    <thead>
        <tr>
            <th>Номер</th>
            <th>Отдел</th>
            <th>Название банка</th>
            <th>Тип счета</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var account in Model)
        {
            <tr>
                <td>@account.Number</td>
                <td>@account.Department.Name</td>
                <td>@account.BankName</td>
                <td>@account.Type</td>
                <td>
                    <a asp-controller="Accounts" asp-action="Details" asp-route-id="@account.Id">Детали</a> |
                    <a asp-controller="Accounts" asp-action="Delete" asp-route-id="@account.Id">Удалить</a>
                </td>
            </tr>
        }
    </tbody>
</table>
